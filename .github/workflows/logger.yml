name: ARC Occupancy Logger
on:
  schedule:
    - cron: "*/15 * * * *"   # every 15 min; commits to data branch = no deploy
  workflow_dispatch:

permissions:
  contents: write

jobs:
  log:
    runs-on: ubuntu-latest
    steps:
      - name: Check out main
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests

      - name: Seed CSV from data branch if it exists
        run: |
          git fetch origin data || true
          if git cat-file -e origin/data:arc_occupancy.csv 2>/dev/null; then
            git show origin/data:arc_occupancy.csv > arc_occupancy.csv
          fi

      - name: Run logger (appends a new reading)
        run: python arc_logger.py --once

      - name: Copy CSV aside, then commit it to the data branch
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          # 1) Save the freshly-updated CSV outside the repo tree
          cp arc_occupancy.csv /tmp/arc_occupancy.csv
          # 2) Switch to a clean data branch (discard main's working changes)
          git checkout -- . || true
          git fetch origin data || true
          if git show-ref --verify --quiet refs/remotes/origin/data; then
            git checkout -B data origin/data
          else
            git checkout -B data
          fi
          # 3) Drop the saved CSV back in and commit just that file
          cp /tmp/arc_occupancy.csv arc_occupancy.csv
          git add arc_occupancy.csv
          git commit -m "Log occupancy $(date -u +'%Y-%m-%d %H:%M UTC')" || echo "No changes"
          git push origin data
